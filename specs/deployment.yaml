apiVersion: apps/v1
kind: Deployment
metadata:
  name: sight
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: sight
        image: docker.pkg.github.com/miguelmoll/sight/sight:latest
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: regcred
